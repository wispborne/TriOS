# Tasks

- [x] Add `isStation` getter to `Ship` model (`hints` contains `STATION`)
- [x] Add "Category" filter (Station / Ship) to `ShipsPageController.build()`
- [x] Fix "Has Modules" in Type filter to use actual module resolution instead of `hasStationSlots`
